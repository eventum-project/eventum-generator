input:
  - cron:
      expression: "*/5 * * * *"
      count: 1

event:
  template:
    params: { }
    samples: { }
    mode: all
    templates:
      - test:
          template: "test.jinja"

output:
  - stdout:
      stream: "${params.stream}"
