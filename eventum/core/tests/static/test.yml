input:
  - static:
      count: 100
  
  - static:
      count: 100
  
event:
  jinja:
    samples: {}
    params: {}
    mode: all
    templates:
      - test:
          template: "template.jinja"

output:
  - file:
      path: "/tmp/eventum_tests/file1.log"
      flush_interval: 0
      write_mode: overwrite
  
  - file:
      path: "/tmp/eventum_tests/file2.log"
      flush_interval: 0
      write_mode: overwrite