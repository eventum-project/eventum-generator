input:
  - static:
      count: 100

  - static:
      count: 100

event:
  template:
    samples: {}
    params: {}
    mode: all
    templates:
      - test:
          template: "template.jinja"

output:
  - file:
      path: "${params.file1}"
      flush_interval: 0
      write_mode: overwrite

  - file:
      path: "${params.file2}"
      flush_interval: 0
      write_mode: overwrite
